{
  "name": "shared-core",
  "version": "1.0.0",
  "description": "판독문에서 추적 관찰 필요 건을 판별하는 공통 코어(플랫폼 독립)",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
